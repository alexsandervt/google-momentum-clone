import styled from "styled-components";

export const Container = styled.div`
  margin: 1rem;
  position: absolute;
`;

export const Temp = styled.p`
  font-size: 4rem;
  font-weight: bold;
  color: #fff;
  margin: 0;
`;
